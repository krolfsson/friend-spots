/** Samma bakgrund som body (#fdf4ff) — inga extra lager som ger kant mot innehållet. */
export function EmojiCollageBackground() {
  return (
    <div aria-hidden className="pointer-events-none fixed inset-0 z-0 min-h-dvh bg-[#fdf4ff]" />
  );
}
